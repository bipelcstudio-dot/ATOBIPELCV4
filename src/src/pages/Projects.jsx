import {useEffect,useState} from "react";import{api}from"../api";
const empty={title:"",description:"",budget:"",start_date:"",deadline:"",priority:"Medium",status:"Planning",progress:0};
export default function Projects(){const[items,setItems]=useState([]),[open,setOpen]=useState(false),[form,setForm]=useState(empty),[error,setError]=useState(""),[busy,setBusy]=useState(false);
 const load=async()=>{try{const d=await api("/projects");setItems(d.items||d.projects||[])}catch(e){setError(e.message)}};useEffect(()=>{load()},[]);
 const set=(k,v)=>setForm(f=>({...f,[k]:v}));async function submit(e){e.preventDefault();setError("");if(!form.title.trim())return setError("عنوان پروژه الزامی است.");setBusy(true);try{await api("/projects",{method:"POST",body:JSON.stringify({...form,title:form.title.trim(),budget:form.budget?Number(form.budget):0,progress:Number(form.progress)||0})});setOpen(false);setForm(empty);await load()}catch(e){setError(e.message)}finally{setBusy(false)}}
 return <section><div className="page-head"><div><small>مدیریت تولید</small><h1>پروژه‌ها</h1></div><button className="primary" onClick={()=>setOpen(true)}>+ پروژه جدید</button></div>{error&&<div className="error">{error}</div>}<div className="cards-list">{items.map(x=><article className="list-card" key={x.id}><div><b>{x.title}</b><p>{x.description||"بدون توضیح"}</p></div><div><span className="badge">{x.status}</span><span className="badge">{x.priority}</span><strong>{Number(x.progress||0)}%</strong></div></article>)}{!items.length&&<div className="empty">هنوز پروژه‌ای ثبت نشده است.</div>}</div>
 {open&&<div className="modal"><form className="modal-card" onSubmit={submit}><div className="modal-head"><h2>پروژه جدید</h2><button type="button" onClick={()=>setOpen(false)}>×</button></div><label>عنوان<input required value={form.title} onChange={e=>set("title",e.target.value)}/></label><label>توضیحات<textarea value={form.description} onChange={e=>set("description",e.target.value)}/></label><div className="form-grid"><label>بودجه<input type="number" min="0" value={form.budget} onChange={e=>set("budget",e.target.value)}/></label><label>شروع<input type="date" value={form.start_date} onChange={e=>set("start_date",e.target.value)}/></label><label>ددلاین<input type="date" value={form.deadline} onChange={e=>set("deadline",e.target.value)}/></label><label>اولویت<select value={form.priority} onChange={e=>set("priority",e.target.value)}><option>Low</option><option>Medium</option><option>High</option><option>Urgent</option></select></label></div><button className="primary" disabled={busy}>{busy?"در حال ثبت...":"ساخت پروژه"}</button></form></div>}</section>}